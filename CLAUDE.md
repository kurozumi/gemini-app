# プロジェクトルール: コエトバ (koetoba)

## 開発ワークフロー
- **GitHub戦略:** すべての機能追加およびコード修正は**プルリクエスト（PR）**を通じて提出する必要があります。
- **PRの説明:** プルリクエストのタイトルと説明は**日本語**で記述し、変更内容に関する詳細な情報を提供してください。
- **自動マージの禁止:** プルリクエストを自動でマージすることは**絶対にしないでください**。
- **承認プロセス:** 修正完了後にPRを作成し、ユーザー（あなた）が内容を確認します。「マージして」と明確な指示があった場合のみ、こちらでマージを実行します。
- **直接プッシュ:** `main` ブランチへの直接プッシュは禁止されています。
- **CI/CD:** すべての PR は GitHub Actions による自動テスト (Lint, Unit, E2E) をパスする必要があります。

## コーディング規約
- **外科的修正:** コードの修正は最小限かつ正確に行い、無関係なリファクタリングを避けること。
- **ベストプラクティス:** Next.js (App Router) と TypeScript の最新の慣習に従うこと。
- **型安全性:** `any` の使用は避け、適切な型定義を行うこと。やむを得ない場合は `eslint-disable` コメントを添える。
- **クリーンアップ:** 未使用のファイル、コンポーネント、インポートは即座に削除し、コードベースを軽量に保つこと。

## 主要コマンド
- ビルド: `npm run build`
- リンター: `npm run lint`
- 単体テスト: `npm run test`
- E2Eテスト: `npm run test:e2e`
- 開発サーバー: `npm run dev`

## これまでの主要な指示と合意
- **セッション管理**: 配信者とリスナーのID衝突を避けるため、Identityに役割を付加する。
- **ルーム削除**: `fetch keepalive`、`pagehide` イベント、LiveKit Webhook、およびトップページ表示時の「実態同期API」による3段構えのクリーンアップ。
- **UI/UX**: ガラスモーフィズムを採用した中央寄せデザイン。レスポンシブ（13インチ等）に対応。
- **配信フロー**: ルーム名設定ステップを追加し、役割選択は自動化。
- **環境設定**: `next.config.ts` は型競合回避のため `next.config.js` に変更済み。
